1) A propriedade "transition" serve para dar uma animação quando houver uma alteração nas propriedades de um elemento, como quando um "input" é focado ou quando o mouse passa por cima do elemento.

2) O elemento sofre um aumento de 5% do seu tamanho original.

3) Para criar pontos de animação por frames para um elemento para ocorrer quando o elemento conter o id ou classe que houver essa animação, e durante esses pontos de animação para um frame específico pode colocar alterações em suas propriedades.

4) Para alterar o texto contido no botão e para colocar uma classe de animação no botão.

5) O GSAP é uma biblioteca de JavaScript para criar movimentos avançado e fluidos nos elementos da página que seriam difíceis ou pesados demais para fazer através do CSS puro.

6) As variáveis guardam valores repetidos da mesma maneira que outras linguagens (como JS e Python) para poder definir uma variável para vários elementos que possam usar a mesma propriedade.

7) Gulp é um automatizador de tarefas focado em executar processos em sequência, como minificar arquivos e compilar Sass), já o Webpack é um empacotador de módulos (bundler) focado em analisar todas as dependências do projeto, como JavaScript, CSS e imagens, por fim, transformando-os em arquivos únicos e otimizados para o navegador.

8) A minificação de arquivos CSS e JavaScript, removendo todos os espaços em branco e quebras de linha do código para deixar os arquivos mais leves e fazer o site carregar mais rápido.
	